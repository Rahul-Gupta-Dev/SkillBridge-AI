backend
│
├── server.js 
const express = require("express");
const cors = require("cors");
require("dotenv").config();

const connectDB = require("./db");

const app = express();

connectDB();

app.use(cors());
app.use(express.json());

app.get("/", (req, res) => {
    res.send("SkillBridge AI Backend is Running");
});

const PORT = process.env.PORT || 5000;

app.listen(PORT, () => {
    console.log(`Server running on port ${PORT}`);
});
const express = require("express");
const cors = require("cors");
require("dotenv").config();

const app = express();

app.use(cors());
app.use(express.json());

app.get("/", (req, res) => {
    res.send("SkillBridge AI Backend is Running");
});

// Courses API
app.get("/api/courses", (req, res) => {
    res.json([
        {
            id: 1,
            name: "Web Development",
            level: "Beginner"
        },
        {
            id: 2,
            name: "Python Programming",
            level: "Intermediate"
        },
        {
            id: 3,
            name: "Artificial Intelligence",
            level: "Advanced"
        }
    ]);
});

// User message API (Chatbot ke liye)
app.post("/api/message", (req, res) => {
    const userMessage = req.body.message;

    res.json({
        reply: "Your question is received: " + userMessage
    });
});

const PORT = process.env.PORT || 5000;

app.listen(PORT, () => {
    console.log(`Server running on port ${PORT}`);
});
├── package.json{
  "name": "skillbridge-ai-backend",
  "version": "1.0.0",
  "description": "Backend for SkillBridge AI project",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  }
}
└── .env
PORT=5000
MONGO_URI=your_mongodb_connection_string